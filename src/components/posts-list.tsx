export default function PostsList() {
  // 1. fetch all posts from database

  // 2. display empty list if no posts

  return (
    <ul>{/* 3. loop over all posts and render a list item for each post */}</ul>
  );
}
